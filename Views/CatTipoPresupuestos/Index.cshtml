@model IEnumerable<WebAdmin.Models.CatTipoPresupuesto>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TipoPresupuestoDesc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUsuarioModifico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaRegistro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdEstatusRegistro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipoPresupuestoDesc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUsuarioModifico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdEstatusRegistro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoPresupuesto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoPresupuesto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoPresupuesto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
